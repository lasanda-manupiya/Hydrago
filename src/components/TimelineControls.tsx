interface Props { minute:number; speed:number; playing:boolean; onPlay:()=>void; onPause:()=>void; onReset:()=>void; onStep:()=>void; onSpeed:(n:number)=>void; onMinute:(n:number)=>void }
export function TimelineControls(p:Props){return <div className='flex flex-wrap items-center gap-2 text-sm'><button className='rounded bg-cyan-700 px-3 py-1' onClick={p.onPlay}>Play</button><button className='rounded bg-slate-700 px-3 py-1' onClick={p.onPause}>Pause</button><button className='rounded bg-slate-700 px-3 py-1' onClick={p.onStep}>Step</button><button className='rounded bg-slate-700 px-3 py-1' onClick={p.onReset}>Reset</button><label>Speed <select className='bg-slate-800 rounded p-1' value={p.speed} onChange={(e)=>p.onSpeed(Number(e.target.value))}><option value={1}>1x</option><option value={2}>2x</option><option value={4}>4x</option></select></label><label className='flex-1'>Minute {p.minute}<input className='w-full' type='range' min={0} max={60} value={p.minute} onChange={(e)=>p.onMinute(Number(e.target.value))}/></label></div>}
