import type { MovementSchedule, MovingObjectType } from '../types/movement';
import type { RouteDefinition } from '../types/routes';
const types: MovingObjectType[] = ['Ground Support Vehicle','Refuelling Vehicle','Maintenance Vehicle','Aircraft','Emergency Vehicle'];
export function MovementControls({ item, routes, onChange }: { item: MovementSchedule; routes: RouteDefinition[]; onChange: (v: MovementSchedule) => void }) {
  return <div className='grid grid-cols-2 gap-2 text-xs'><select className='bg-slate-800 p-1 rounded' value={item.type} onChange={(e)=>onChange({...item,type:e.target.value as MovingObjectType})}>{types.map(t=><option key={t}>{t}</option>)}</select><select className='bg-slate-800 p-1 rounded' value={item.routeId} onChange={(e)=>onChange({...item,routeId:e.target.value})}>{routes.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><input className='bg-slate-800 p-1 rounded' type='number' value={item.startTime} onChange={(e)=>onChange({...item,startTime:Number(e.target.value)})}/><input className='bg-slate-800 p-1 rounded' type='number' value={item.repeatInterval} onChange={(e)=>onChange({...item,repeatInterval:Number(e.target.value)})}/><input className='bg-slate-800 p-1 rounded' type='number' value={item.speed} onChange={(e)=>onChange({...item,speed:Number(e.target.value)})}/><input className='bg-slate-800 p-1 rounded' type='number' value={item.repeatTrips} onChange={(e)=>onChange({...item,repeatTrips:Number(e.target.value)})}/></div>;
}
